// bankTransfer.ts

export function bankTransfer(amount: number, accountNumber: string): string {
    // Logic for handling bank transfer payments
    return `Transfer of ${amount} to account ${accountNumber} has been initiated.`;
}